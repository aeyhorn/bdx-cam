# Implementation blocked

Edits to code files are blocked while **Plan mode** is active (only markdown can be edited).

To have the assistant apply the full CRUD + workflow implementation:

1. **Switch to Agent mode** (or turn off Plan mode), then ask again: *„setze den Plan um“* or *„implementiere das“*.

Alternatively, copy the plan from the chat and implement manually.
